// TODO - Figure out if this can be done, can't figure out right now because it is JSON file. Would like to somehow export this to be imported and used in the games.

const defaultGameJsconfig = {
    "compilerOptions": {
        "paths": {
            "@/*": ["./*"]
        }
    }
}

export default defaultGameJsconfig;